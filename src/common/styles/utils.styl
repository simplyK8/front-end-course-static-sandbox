.absolute-center {
  position absolute
  top 50%
  left 50%
  transform: translate(-50%, -50%)
}

.animation-rotation {
  animation-name rotation
  animation-duration: 3000ms
  animation-iteration-count: infinite
  animation-timing-function linear
}

@keyframes rotation {
    0% {
      transform: translate(-50%, -50%) rotate(0deg)
    }
    100% {
      transform: translate(-50%, -50%) rotate(-360deg)
    }
}
body {
  font-family: "Didact Gothic", sans-serif;
  background-color: $evaColor;
}

.row {
  display: flex;
  justify-content: space-between;
  align-items: center;
}
.text-eva {
  color: $evaColor
  }

.text-title {
  font-family: 'Black Han Sans', sans-serif;
}

.text-center {
  text-align: center;
}

.text-white {
  color: $plainWhite;
}

.section {
  height: 100vh;
  padding: $bigSpacer;
}


.bg-tertiary {
  background-color: $tertiaryColor;
}

.bg-secondary {
  background-color: $secondaryColor;
}

#home {
  padding-top: 50px;
}

.container {
  margin: auto;
  max-width: 1130px;
  height: 50%;
}

.card {
  padding: $bigSpacer;
  background: $plainWhite;
  width: 300px
  height: 300px
}

.card-narrow {
  width: 200px;
}

.standard-border {
  border: 1px solid $primaryColor;
}

.fancy-input {
  background: $plainBlack;
  border: 1px solid $plainBlack;
  outline: none;
  height: 34px;
  width: 100%;
  padding: $smallSpacer;
  box-sizing: border-box;
}

.fancy-input:focus {
  background: $tertiaryColor;
  border: 1px solid $primaryColor;
}

.btn-primary,
.btn-secondary {
  cursor: pointer;
  border: none;
  width: 150px;
  height: 36px;
  line-height: 36px;
  transition: all ease-in-out 300ms;
  &:hover {
    box-shadow: 2px 2px 14px 0 $tertiaryColor
  }
}

.card-hover {
  cursor: pointer;
   &:hover {
    box-shadow: 2px 2px 14px 0 $glow
  }
  }

.btn-primary {
  color: $plainWhite;
  background: $primaryColor;
}

.btn-secondary {
  color: $primaryColor;
  background: $plainWhite;
}

.d-block {
  display: block;
}
.m-b {
  margin-bottom: $standardSpacer
  }
.m-t {
  margin-top: $standardSpacer;
}

.m-r {
  margin-right: $standardSpacer;
}

.m-l {
  margin-left: $standardSpacer;
}

.m-l-sm {
  margin-left: $smallSpacer;
}

.p-t {
  padding-top: $standardSpacer;
}

.navigation {
  height: 50px;
  background: $primaryColor;
  color: $plainWhite;
  width: 100%;
  position: fixed;
  top: 0;
  padding: 0 10px;
}

.navigation-item {
  padding: 15px;
  color: $plainWhite;
}

.avatar {
  width: 100px;
  height: 100px;
  border-radius: 50%;
  object-fit: cover;
  
}

.badge {
  width: 50px;
  height: 50px;
  border-radius: 50%;
  object-fit: cover;
  
  }

.info {
  border-bottom: 1px solid $primaryColor;
  padding: $standartPadding 0;
  color: $tertiaryColor;
}

.text-accent {
  color: $accentColor;
}

.text-black {
  color: $plainBlack;
}

.round-border {
  border-radius: 8px;
}

.drop-shadow {
  box-shadow: 2px 2px 4px 0 $shadow
}

.m-auto {
  margin-left: auto
  margin-right: auto
}
.wrap {
  flex-wrap: wrap
  }

  .card-text {
    font-family: 'Raleway', sans-serif;
    color: $cardText
    }
   